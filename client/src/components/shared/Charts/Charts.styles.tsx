import styled from 'styled-components'

export const StyledCharts = styled.div`
    display: grid;
    overflow-x: auto;
    >div {
        overflow-x: auto;
        overflow-y: hidden;
        display: grid;
        width: 100%;
    }
`